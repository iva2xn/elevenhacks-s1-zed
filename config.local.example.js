/**
 * Local config template — copy this to config.local.js and fill in your keys.
 * config.local.js is gitignored and will not be committed.
 */
export const LOCAL_CONFIG = {
  elevenLabsApiKey: 'YOUR_ELEVENLABS_API_KEY_HERE'
};
